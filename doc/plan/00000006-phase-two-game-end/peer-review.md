# Peer Review — Story 00000006: Phase 2 game-end (victory, draws & losses)

## Summary

The branch completes Phase 2: the Flag becomes an attackable target (`movement.ts`,
`combat.ts`), structural reachability and the Unbreachable-Flag inputs land in a new
`reachability.ts`, game-end detection with the reference engine's precedence lands in a new
`outcome.ts`, the two counters and `result` join `PlayState` (with `agreeDraw`), the record
gains `Result`/`ResultReason`, and the session/UI layers gain inertness, the draw-offer flow
(with the owner's responder-perspective board flip via `viewSide`), the countdown-warning
banner, the end-of-game panel and the New game action. Verification: `npm run typecheck`
**passes**, `npm run lint` **passes** (no findings), `npm test` **passes** (347 tests,
18 files). The implementation plan does include a README accuracy step (Step 17), and the
README was updated — but the new player-facing description of the Unbreachable-Flag win is
stated backwards (comment 1). The two owner deviations called out for this review (Step 13's
`viewSide` perspective flip, Step 12's unnecessary sacrifice note) are recorded in both the
plan's Notes and `story.md`, and the code matches them; rule logic, precedence, and counter
semantics otherwise track the plan's grounding facts exactly.

## Comments

### Major

| #   | Status | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Location                                                | Comment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Suggested Change                                                                                                                                                                                                                                                                                                 | Code Snippet                                                                                                 |
| --- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| 1   | Fixed  | Fixed. README now states the win correctly: your **own** flag becoming impossible for the opponent to reach. Kept in players' language.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [README.md#L21-L23](../../../README.md#L21-L23)         | The new "Win, lose, or draw" bullet describes the Unbreachable-Flag win (§6.2) **backwards**. The rule (and the code — `outcome.ts`'s `whiteWinsUnbreachable = whiteFlagEnclosed && !blackSappersAvailable`) awards the win to the player whose **own** Flag is enclosed while the **opponent** has no Sapper able to breach it. The README tells players they win "if **their** flag becomes impossible to reach" — the opposite claim, and one that would mislead a player into thinking an unreachable enemy Flag is a win. Step 17 was the step whose purpose was README accuracy, so this is a miss against the plan's own final step.                                                                                                | Reword to describe the win as your own Flag becoming unreachable, e.g. "You can also win if your own flag becomes impossible for your opponent to reach — walled in, with none of their sappers able to break through — or if they are left with no legal move at all."                                          | `You can also win if their flag becomes impossible to reach, or if they are left with no legal move at all.` |
| 2   | Fixed  | Fixed, with an owner decision on the focus target. Focus moves to a **button**, never the sentence: `GameResult` focuses New game, `DrawOffer` focuses Decline while an offer is pending (and Offer a draw on a decline). Focusing the sentence would have made a screen reader speak it twice, since the live region already carries it - the double-announce Step 16 explicitly ruled out. Decline (not Accept) is the pending-offer target so a stray Enter is harmless. Note the review overstated the scope: a game ending from a board move does **not** strand focus, as the board cell keeps it; only an unmounting button does. | [src/App.tsx#L144-L186](../../../src/App.tsx#L144-L186) | Keyboard focus is dropped on every draw-offer transition and at game end. Activating "Offer a draw" unmounts that button (replaced by the prompt panel), activating Accept unmounts the prompt (replaced by `GameResult`), and a game-ending board activation swaps `PlayStatus`/`DrawOffer` for `GameResult` — in each case the focused element disappears and focus falls back to `<body>`, so the next Tab restarts from the top of the document. Nothing moves focus (no `.focus()` / `tabIndex={-1}` target exists outside `AccessibleGrid`). The flow is still operable (Gate F passed), but story item 9 asks for the endings to be operable by keyboard as first-class, and the responder/winner has to hunt for the new controls. | Move focus to the newly rendered panel (a `tabIndex={-1}` heading or the primary button) when `drawOffer` changes or the game ends — Step 16 already sanctioned this fix, with the caveats that it must not trap focus and must not cause the result to be announced twice (so focus a non-live-region element). | `setPlaySession(offerDraw(playSession));` … `<GameResult result={result} onNewGame={handleNewGame} />`       |

### Minor

| #   | Status | Resolution                                                                                                                                                                                                                                                                                                       | Location                                                                                    | Comment                                                                                                                                                                                                                                                                                                                                                                                       | Suggested Change                                                                                                                                                                                                                | Code Snippet                                                                               |
| --- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| 3   | Fixed  | Fixed. `attackTargets` now returns `[]` when `isInert(session)`, by contract rather than by accident, matching `actionableSquares`/`activatableSquares`. Covered by a new test.                                                                                                                                  | [src/board/playSession.ts#L202](../../../src/board/playSession.ts#L202)                     | `attackTargets` is the only one of the four selectors not gated by `isInert`. It is safe today only because `offerDraw` clears the selection and a completed/ending ply leaves `selection === null`, so it returns `[]` by accident rather than by contract — a future change that preserved a selection would leak attack highlights onto an inert board.                                    | Add the same `if (isInert(session)) return [];` guard as `actionableSquares`/`activatableSquares`, and add a test asserting it is empty when the game is over.                                                                  | `export function attackTargets(session: PlaySession): Square[] {`                          |
| 4   | Fixed  | Fixed. `sideColorName` extracted to `src/board/sideNames.ts` and imported by all six modules; the side-flip helper is now a single exported `otherSide` in `board.ts`, replacing the two rule-layer `OTHER_SIDE` maps and the UI copies. Behavior-preserving.                                                    | [src/board/DrawOffer.tsx#L38-L45](../../../src/board/DrawOffer.tsx#L38-L45)                 | `sideColorName` is now copy-pasted into six modules (`PlacementStatus`, `PlayStatus`, `PlayBoard`, `playAnnouncement`, `playWarnings`, `DrawOffer`) and `otherSide` into three UI modules plus two rule-layer copies. This story added two more copies of each. Player-facing color naming is a cross-cutting convention; six copies invite divergence.                                       | Extract a single shared helper (e.g. `src/board/sideNames.ts` exporting `sideColorName`, and reuse the rule layer's side-flip helper) and have the components import it rather than redeclare it.                               | `function sideColorName(side: Side): string { return side === "white" ? "Red" : "Blue"; }` |
| 5   | Fixed  | Fixed. `DrawOffer` renders `describeDrawOffer(drawOffer)` instead of rebuilding the sentence, so the visible prompt and the announced sentence cannot drift.                                                                                                                                                     | [src/board/DrawOffer.tsx#L83](../../../src/board/DrawOffer.tsx#L83)                         | The visible prompt ("Red offers a draw. Blue, **do you accept?**") and the announced sentence from `describeDrawOffer` ("Red offers a draw. Blue, **accept or decline?**") are composed independently and do not match. A screen-reader user hears one wording and a sighted partner reads another; the two can drift further apart since neither has the other as its source.                | Have `DrawOffer` render `describeDrawOffer(drawOffer)` (the already-unit-tested wording) rather than rebuilding the sentence, as `GameResult` does with `describeResult`.                                                       | `{offerer} offers a draw. {opponent}, do you accept?`                                      |
| 6   | Fixed  | Fixed per the owner's decision: while an offer is pending, `PlayStatus` reads "Red's turn — draw offer pending" (Blue equivalent likewise). Turn information stays visible rather than being hidden.                                                                                                             | [src/App.tsx#L173-L182](../../../src/App.tsx#L173-L182)                                     | While a draw offer is pending, `PlayStatus` still reads "{Offerer} to move" even though the board is now drawn from the **responder's** perspective (Step 13's owner decision). Literally correct, but at the hot-seat hand-off the responder is looking at a board oriented for them and a status line naming the other player as the mover.                                                 | Consider suppressing or rewording `PlayStatus` while `drawOffer !== null` (e.g. hide it, or show "Red's turn — draw offered"), so the two status elements do not appear to contradict each other.                               | `<PlayStatus sideToMove={playSession.play.sideToMove} />`                                  |
| 7   | Fixed  | Fixed per the owner's decision: the result sentences now name the losing side in plain language - "Blue wins — Red ran out of moves without attacking.", "Red wins — Blue has no legal move left.", "Red wins — Blue can no longer reach Red's flag." `playAnnouncement.test.ts` updated to pin the new wording. | [src/board/playAnnouncement.ts#L117-L123](../../../src/board/playAnnouncement.ts#L117-L123) | The result sentences are terse for a non-technical player: "Blue wins — Inactivity." does not say whose clock ran out, "Red wins — No legal move." does not say who was stuck, and "Unbreachable Flag" is exactly the jargon the README deliberately avoided ("impossible to reach"). The story asks that both players be told "plainly … who won … and why".                                 | Expand the reason clauses to name the losing side and avoid rules jargon, e.g. "Blue wins — Red ran out of moves without attacking." / "Red wins — Blue has no legal move." / "Red wins — Blue can no longer reach Red's flag." | ``return `${sideColorName(result.winner)} wins — ${reasonPhrase(result.reason)}.`;``       |
| 8   | Fixed  | Fixed. `story.md`'s Background now matches what shipped: an ongoing record carries `[Result "*"]` and no `ResultReason` tag (owner's decision of 2026-07-11).                                                                                                                                                    | [doc/plan/00000006-phase-two-game-end/story.md#L66-L70](./story.md#L66-L70)                 | Story/plan discrepancy: the story's Background states the record format "always writes `Result` … **and** `ResultReason`", while the plan (Grounding facts, and Step 5) records an owner decision of 2026-07-11 to **omit** `ResultReason` entirely while a game is ongoing — which is what shipped. The story was never updated, so the two reference documents disagree on a format detail. | Update `story.md`'s Background bullet to match the owner's decision (ongoing records carry `[Result "*"]` and no `ResultReason`), so the story and plan agree with the implementation.                                          | `always writes `Result`… and`ResultReason` … header tags.`                                 |
