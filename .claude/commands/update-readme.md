Review the current branch diff and update `README.md` if warranted.

README.md is written for players of the game first, developers second. It should reflect the project at a high level — what the game is, what the app lets a player do, and (briefly) how development works. Update it only if the changes in this branch affect something a player or a new reader of the project would need to know. Good candidates: a new player-visible capability (playing, replaying, new game features), changed setup steps, a change to which ruleset versions are supported, removed or renamed entry points.

Do not update it for: internal refactors, bug fixes, test changes, performance tweaks, or implementation details that don't change what a player or reader sees. The README should grow slowly and deliberately — no change is a perfectly valid outcome. Keep its tone accessible to a non-technical reader; technical detail belongs in CONTRIBUTING.md.

After reviewing the diff, either apply the minimal edits needed or, if no update is warranted, briefly explain why and make no changes.
